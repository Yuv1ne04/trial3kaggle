# Dependencies for the Sentinel-2 cloud-reconstruction preprocessing pipeline.
# Targeted at Python 3.11+ (tested on 3.13).

# Core (all modules)
rasterio>=1.3
numpy>=1.24
pandas>=2.0
tqdm>=4.65

# Cloud detection (s2cloud)
s2cloudless>=1.7
scipy>=1.10
opencv-python-headless>=4.8
lightgbm>=4.0

# Reference selection (s2refselect)
pyyaml>=6.0
